{
  "name": "resume-generator-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "cd resume-builder-backend && node src/index.js",
    "build": "cd resume-builder-backend && npm install",
    "dev": "cd resume-builder-backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
